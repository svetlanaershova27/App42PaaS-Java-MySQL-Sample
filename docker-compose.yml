version: '3'
services:
  builder:
    container_name: maven2
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8082:8080"

  mysql:
    container_name: db
    image: mysql
    ports:
      - "9934:5432"
    expose:
      - "5432"
    environment:
      - MYSQL_ROOT_PASSWORD=pass
    volumes:
      - ./mysql:/vat/lib/mysql


  runner:
    container_name: tomcat2
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8091:8080"
    links:
      - mysql
    depends_on:
      - "mysql"







